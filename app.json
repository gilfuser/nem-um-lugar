{
  "name": "nem-um-lugar",
  "description": "OSC trough for non-local jams",
  "scripts": {
  },
  "env": {
    "PUSHER_APP_ID": {
      "required": true
    },
    "PUSHER_APP_KEY": {
      "required": true
    },
    "PUSHER_APP_SECRET": {
      "required": true
    },
    "PUSHER_CLUSTER": {
      "required": true
    }
  },
  "formation": {
    "web": {
      "quantity": 1
    }
  },
  "addons": [

  ],
  "buildpacks": [
    {
      "url": "heroku/nodejs"
    }
  ]
}
